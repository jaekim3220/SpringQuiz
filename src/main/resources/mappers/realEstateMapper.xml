<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 
<!-- DB영역 -->
<mapper namespace = "com.quiz.lesson03.mapper.RealEstateMapper">

	<!-- lesson03 MyBatis 1-1 문제 : id 로 select 하기 -->
	<select id="selectRealEstateById" parameterType="int" resultType="com.quiz.lesson03.domain.RealEstate">
		SELECT
			`id`,
			`realtorId`,
			`address`,
			`area`,
			`type`,
			`price`,
			`rentPrice`,
			`createdAt`,
			`updatedAt`
		FROM 
			`real_estate`
		WHERE 
			`id` = #{id}
	</select>
	
	<!-- lesson03 MyBatis 1-2 문제 : 월세 조건 select -->
	<select id="selectRealEstateListByRentPrice" parameterType="int" resultType="com.quiz.lesson03.domain.RealEstate">
		SELECT
			`id`,
			`realtorId`,
			`address`,
			`area`,
			`type`,
			`price`,
			`rentPrice`,
			`createdAt`,
			`updatedAt`
		FROM 
			`real_estate`
		WHERE
			<!-- > : &gt; -->
			<!-- rentPrice &gt; #{rent_price} -->
        	<!-- < : &lt; -->
        	<!-- rentPrice &lt; #{rent_price} -->
	        <![CDATA[
	        	`rentPrice` < #{rent_price}
	        ]]>
	</select>
	
	<!-- lesson03 MyBatis 1-3 문제 : 복합조건 select -->
	<select id="selectRealEstateListByAreaPrice" parameterType="map" resultType="com.quiz.lesson03.domain.RealEstate">
		SELECT
			`id`,
			`realtorId`,
			`address`,
			`area`,
			`type`,
			`price`,
			`rentPrice`,
			`createdAt`,
			`updatedAt`
		FROM 
			`real_estate`
		WHERE
	        <![CDATA[
	        	`type` = '매매' AND
	        	`area` >= #{area} AND
	        	`price` <= #{price}
	        	
	        ]]>
	</select>
	
	
	<!-- lesson03 MyBatis 2-1 문제 : 객체로 insert 하기 -->
	<insert id="insertRealEstate" parameterType="com.quiz.lesson03.domain.RealEstate">
		INSERT INTO `real_estate`
		(
			`realtorId`,
			`address`,
			`area`,
			`type`,
			`price`,
			`rentPrice`,
			`createdAt`,
			`updatedAt`
		)
		VALUES
		(
		#{realtorId},
		#{address},
		#{area},
		#{type},
		#{price},
		#{rentPrice},
		NOW(),
		NOW()
		)
	</insert>
	
	<!-- lesson03 MyBatis 2-2 문제 : field 로 insert 하기 -->
	<insert id="insertRealEstateAsField" parameterType="map">
		INSERT INTO `real_estate`
		(
			`realtorId`,
			`address`,
			`area`,
			`type`,
			`price`,
			`rentPrice`,
			`createdAt`,
			`updatedAt`
		)
		VALUES
		(
		#{realtorId},
		#{address},
		#{area},
		#{type},
		#{price},
		#{rentPrice},
		NOW(),
		NOW()
		)
	</insert>
	
</mapper>